const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));const mediaDialog=document.querySelector('.media-dialog');const videoFrame=document.querySelector('.video-frame');document.querySelectorAll('[data-video]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.video;videoFrame.innerHTML='<iframe src="https://www.youtube-nocookie.com/embed/'+encodeURIComponent(id)+'?autoplay=1" title="Guitar Musicians video player" allow="autoplay; encrypted-media; picture-in-picture" allowfullscreen></iframe>';mediaDialog.showModal()}));mediaDialog?.querySelector('.dialog-close').addEventListener('click',()=>{mediaDialog.close();videoFrame.innerHTML=''});mediaDialog?.addEventListener('click',event=>{if(event.target===mediaDialog){mediaDialog.close();videoFrame.innerHTML=''}});const imageDialog=document.querySelector('.image-dialog');const dialogImage=imageDialog?.querySelector('img');document.querySelectorAll('.gallery-item').forEach(button=>button.addEventListener('click',()=>{dialogImage.src=button.dataset.full;dialogImage.alt=button.querySelector('img').alt;imageDialog.showModal()}));imageDialog?.querySelector('.dialog-close').addEventListener('click',()=>imageDialog.close());imageDialog?.addEventListener('click',event=>{if(event.target===imageDialog)imageDialog.close()});const serviceDialog=document.querySelector('.service-dialog');const serviceImage=serviceDialog?.querySelector('.service-dialog-image');const serviceTitle=serviceDialog?.querySelector('#service-dialog-title');const serviceText=serviceDialog?.querySelector('.service-dialog-text');document.querySelectorAll('.service-trigger').forEach(button=>button.addEventListener('click',()=>{serviceImage.src=button.dataset.image;serviceImage.alt=button.dataset.alt;serviceTitle.textContent=button.dataset.title;serviceText.textContent=button.dataset.copy;serviceDialog.showModal()}));serviceDialog?.querySelector('.dialog-close').addEventListener('click',()=>serviceDialog.close());serviceDialog?.addEventListener('click',event=>{if(event.target===serviceDialog)serviceDialog.close()});document.querySelectorAll('dialog').forEach(dialog=>dialog.addEventListener('close',()=>{if(dialog===mediaDialog)videoFrame.innerHTML=''}));document.querySelector('#year').textContent=new Date().getFullYear();
